# BRAHMO Composition Agent - Python Dependencies
# Backend API and composition engine requirements

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Database
supabase>=2.10.0

# Environment & Configuration
python-dotenv>=1.0.0

# Token Counting (Claude-compatible cl100k_base encoding)
tiktoken>=0.8.0

# Data Validation (FastAPI dependency, but pinned for stability)
pydantic>=2.0.0

# Additional utilities that may be needed
httpx>=0.27.0  # For async HTTP requests (used by supabase)